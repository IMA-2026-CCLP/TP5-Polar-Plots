# ── Alineador Temporal de Tomas Múltiples ─────────────────────────────────────
# Instalación: pip install -r requirements.txt
#
# Python >= 3.10 requerido (se usa `X | Y` en type hints)

# ── Obligatorias ───────────────────────────────────────────────────────────────
numpy>=1.24
scipy>=1.10
soundfile>=0.12
PyQt6>=6.4
pyqtgraph>=0.13

# ── Opcionales ─────────────────────────────────────────────────────────────────
# Tema visual oscuro (la app funciona sin esto, usa estilo propio como fallback)
qt-material>=2.14

# Reproducción de audio con el botón Play
# (si no está instalado, el botón queda deshabilitado automáticamente)
sounddevice>=0.4
